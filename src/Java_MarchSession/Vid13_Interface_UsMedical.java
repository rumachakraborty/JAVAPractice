package Java_MarchSession;

public interface Vid13_Interface_UsMedical {
//Interface we never write main method
	//No method body will be there in interface
	//only method prototype
	//no static method in interface->we cannot over ride sttaic method->but over riden is complusory in
	//but interface variable are static and final in nature by default
	//cannot create the object for interface
	
	
	int min_fee=10;
	public void us_orthopedicServices();
	public void us_dentalServices();
	public void us_nueroServices();
	public void us_AmbulanceServices();
}
